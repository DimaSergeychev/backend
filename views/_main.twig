{% extends "layout.twig" %}
{% import "macros.twig" as macros %}
{% from "macros.twig" import btn %}

{% block content %}
Вы находитесь на главной странице :)
{% block mainContent %}
<ul class="list-group mt-3">
    <li class="list-group-item">
      {% for item in menu_Toyota %}
      <a>{{ macros.btn(item.title, item.url, title == item.title) }}</a>
      {% endfor %}
      </li>
    <li class="list-group-item">
      {% for item in menu_BMW %}
      <a>{{ macros.btn(item.title, item.url, title == item.title) }}</a>
      {% endfor %}
    </li>
  </ul>
{% endblock %}
{% endblock %}